# CONTEXT.md — 领域词汇表

> 本文件是项目的**统一语言词汇表**（glossary），只收录领域概念的名字与含义，不含任何实现细节。
> 实现与架构见 `AGENTS.md`；决策记录见 `AGENTS.md` 设计约定（ADR 列表）。

## 内容创作执行面

| 术语                               | 含义                                                                                                           |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **世界书宏**                       | 世界书正文中的 `{{...}}` 文本指令，由宏管线处理；它不是 EJS，输出美化 replacement 也不会处理它。               |
| **EJS 代码块**                     | 世界书条目正文里的 `<%...%>` 计算单元，在提示装配时运行；不要用这个名字指输出美化脚本。                        |
| **EJS 能力面**                     | 世界书 EJS 可依赖的作者接口总称，包括 `stats`、`vars`、`local` 等；与游戏机制的 `$` API 和存量兼容别名相区分。 |
| **EJS 共享局部空间**（`local`）    | 当前存档内由所有世界书 EJS 共用的小型状态空间；不是项目隔离、浏览器 localStorage 或秘密存储。                  |
| **输出美化规则**                   | 对已提交 assistant 正文做显示侧正则替换的规则；不改写 Agent 提示或游戏状态。                                   |
| **富替换内容**（replacement）      | 输出美化规则产生的 HTML/CSS/JS 内容；它是字面浏览器文档内容，不是 EJS 模板，也没有世界书宏 pass。              |
| **正则共享空间**（`regexStorage`） | 所有输出美化规则、项目、存档和预览共同读写的持久状态域；与应用数据和 EJS `local` 分离。                        |
| **富匹配文档**                     | 一次输出美化正则命中的 replacement 所拥有的隔离文档；未命中正文留在宿主原生文本面，相邻命中也各自独立。        |

## 地图系统

| 术语             | 含义                                                                                                                    |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **位置路径**     | 玩家/角色位置的**唯一真源**：AI 书写的自由文本连字符路径（大陆-国家-城市-街区）。地图系统永不改写它的地位。             |
| **地块**         | 地图的最小地理单元（手绘省份）。有名字、地形、水域/不可通行标记、静态所有者与邻接关系。比场景粗：同城内换街区不换地块。 |
| **落位**         | 把位置路径投影到地块的解析动作。只做精确/归一化/绑定表匹配，**绝不模糊匹配**；失败时保持上一次落位结果不动，绝不瞎猜。  |
| **绑定名字空间** | 落位能识别的全部名字集合。AI 的上下文必须持续展示真实地块名（当前块 + 邻接块），使 AI 书写的位置路径天然落在此空间内。  |
| **锚地块**       | 一个中层/国家的默认到达地块（首府所在块）。位置路径只写到中层粗度时：已在域内则原地不动，域外则落到锚地块。             |
| **旅行目的地**   | 队伍当前宣告的远行目标。可由玩家在地图上选定，也可由叙事宣告；到达或放弃即消失。                                        |
| **计划路线**     | 通往旅行目的地的建议性路线。仅供展示与天数估算；叙事偏离它时按新位置重估，绝不强制执行。                                |
| **地块事实**     | AI 叙事在地块上留下的既成事实（状态/发展度/建筑/编年史）。与落位派生态相反：**永不**随换包清空，名字消失只休眠不删。    |
| **地块状态**     | 挂在地块上的限时或永久事记（标题+描述+效果+时长），标题为地块内逻辑键，同名再挂即整条刷新。与角色状态效果无关。         |
| **发展度**       | 陆地块的发展水平：10 个带名字的档 + 一条进度条。进度只被叙事推动（周期效果或一次性授予），没有后台自然增长。            |
| **建筑槽**       | 地块可容纳建筑的编号槽位，槽数恒等于发展档数。严格槽位身份：降档移除最高号槽并摧毁其中建筑，空槽与玩家产业不豁免。      |
| **玩家产业**     | 归玩家所有的建筑，唯一有真实机制效果的所有权形态（按期金钱收益）。取得只经叙事，不存在直买按钮。                        |
| **主建筑**       | 每个陆地块恰有一座、代表主聚落的建筑。不占编号槽、降档免疫、不可摧毁；名字作者可命、缺席按档派生通名。                  |
| **地块编年史**   | 地块上带游戏内日期的大事记：五类自动事件 + AI 附注。定容保留（首访条目钉住不淘汰），自动条目不可编辑。                  |
| **时间账本**     | 回合驱动的到期调度：游戏时间前进时从事实锚点纯推导「谁到期了」，集中调度、分散结算。它不是自走时钟。                    |

## 战斗系统 v3

| 术语                       | 含义                                                                                                                                      |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| **内核**（Kernel）         | 战斗期间主持流程、判定数值事实的确定性权威。对外只有「开一场战斗 → 提交命令 → 拿一次状态迁移」这一条面，不暴露推进回合或直改数值的原语。  |
| **骰带**（DiceTape）       | 内核持有的随机源：一次输出发下来的骰子按用途分成若干条通道，各自顺序消费。它是「所有随机数来自内核」这条不变量的载体。                    |
| **骰池**                   | 一次正文输出所配发的那一批 d20。耗尽即整批作废、向外要新的一批（续杯），上一批余骰不跨批复用。                                            |
| **续杯**                   | 骰池耗尽后注入全新一批骰子的动作。它是常规路径而非边界情况，因此有对应的等待态。                                                          |
| **EffectIntent**           | 效果对内核**声明意图**的封闭词汇（改数值/请求结算/覆写规则/授权/改目标/排程/召唤/请求选择）。效果永远只提意图，不自己改状态。             |
| **窗口**（ReactionWindow） | 结算流程中预留给效果介入的具名时机。窗口内只读快照、只回意图，不能推进流程。清单封闭为 18 个，其中 12 个已接求值器，其余 6 个为保留声明。 |
| **接线 / 保留**            | 窗口的两种状态：接线=有求值器、订阅它的效果真的会跑；保留=只有名字，订阅它在编译期即被拒（而非静默不生效）。                              |
| **RuleKey**                | 内核开放给「法则级」能力改写的**封闭**规则位（战意强制/终局强制/冻结行动槽/死亡阈值）。不存在任意规则改写：不在册的规则位就是不可改写。   |
| **EffectAutomaton DSL**    | 效果的声明式表达形式：订阅一个窗口 + 一条封闭微文法的触发条件 + 一组意图模板。战斗内以它取代任意脚本，保证可重放。                        |
| **登神强度**（divinity）   | 效果/装备/技能的规则位阶。决定求值先后、能否动用 RuleKey，以及对抗中的压制幅度。                                                          |
| **战斗主持人**（DM 模式）  | 战斗 Agent 的定位：一个角色贯穿全场，既忠实解析玩家的意图文本、又扮演敌方决策、还负责结算演绎与终局摘要。它不是「敌方专属决策器」。       |
| **会话模式**               | 战斗 Agent 的组织方式：整场战斗共用一条持久会话与一份消息数组，system 只发一次；查询类工具与命令类工具严格分流，前者取数、后者产命令。    |
| **投影**（projection）     | 内核状态对外的两张只读面：给前端的事件流投影与给 Agent 的文本面板投影。两面都由唯一权威派生，谁都拿不到可变引用。                         |
| **有界裁决**               | 现有意图词汇表达不了的开放性创意，交由 Agent 提方案、内核按门槛校验后接纳的受控通道。它是词汇缺口的出口，不是绕过内核的后门。             |

## 随机事件系统

> 「事件」在本仓另有三个占用者：**剧情大事件**（剧情大纲树上的节点，由剧情系统管理）、**引擎状态变更事件**（EventBus 上的 GameEvent）与**战斗事件**（战斗内核产出的既成事实 DomainEvent，及其投影给前端的 CombatEvent——后者仍是 v2 留下的形状）。本节词汇专指随机事件子系统，四者不可混用。

| 术语                        | 含义                                                                                                                                             |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| **随机事件**                | 由确定性调度按概率或首访解锁、交给主叙事 AI 在叙事方便时演绎的一次性遭遇。不是剧情大事件，也没有任务那样的进行中状态机。                         |
| **随机事件定义**            | 内容侧的事件模板（名字/触发方式/门槛/权重/槽位/简报），纯数据；逻辑键是名字，AI 永不见 id。                                                      |
| **候选池**                  | 已判定可触发、等待 AI 认领的事件实例集合。跨回合驻留，有数量上限与时效。                                                                         |
| **入池**                    | 概率掷中或首访强制，把一条定义实例化放进候选池的动作；组装槽位在此刻采样定型。                                                                   |
| **触发（认领）**            | AI 把候选事件编织进正文并回执认领的动作。一次触发清空其余非强制候选，并起动全局冷却。                                                            |
| **全局冷却**                | 任一事件触发后，全部概率掷骰暂停的共享冷却期。首访强制不受其约束。                                                                               |
| **首访事件**                | 作者点名的地点第一次到达时强制入池的事件；地点足迹于触发时（而非入池时）记账。未被点名的新地点不起事件。                                         |
| **足迹**                    | 已触发过首访事件的地点集合。                                                                                                                     |
| **可用性门槛**（available） | 事件是否进入考虑范围的硬条件：不满足则不掷骰、不入首访选择、在池即撤。与情境权重相区分：门槛=事件不存在于当前局面，权重=事件存在但此刻概率为零。 |
| **情境权重**                | 按当前局面（地点/等级/在途/变量等）对事件触发概率的乘法修正；乘到零表示此时此地不会发生，但事件仍然存在。                                        |

## 主线细化层（2026-09-09 / ADR-35）

> 与「剧情大事件」及「随机事件」的边界：细化层是主线的**投影**（宏观方向的微观实例化），
> 事件树是主线的**骨架**（窗口到达才触发），随机事件是主线的**题外话**。三者互不替代。

| 术语                       | 含义                                                                                                                                                                      |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **主线细化节点**           | 空白期由 pre_check 现编的一条「谁、因为什么、做了什么」，住在 `worldFlags.plotThreads`，按节点名寻址。它是世界的运转片段，不是任务、不阻塞主线、不写时间。                |
| **事件线**（plot threads） | 全部节点与伏笔连线构成的有向图；边不独立存储，由 `foreshadows`（埋向）与 `payoffs`（回收）两组引用推导并去重。                                                            |
| **细化闸门**（gate）       | Code 的节奏判据：main-only、全存档 4 成功回合冷却、窗口距离概率带（0.15/0.30/0.50/0.70）、专用 salt 确定性抽样（同回合重试不重掷）。闸门只控制「新建/推进」，不禁止结算。 |
| **主线锚**（thread）       | 节点挂靠的大纲标题/章节标题/事件标题（逐字一致的精确名字集合）；`directionAnchors` 是自由文本，不拆成虚构标题。                                                           |
| **窗口距离**               | 当前游戏时间到最近未来 pending 事件窗口起点的天数（月窗口从当月首日起算）；空白期（有 active 事件或已进窗口）关闭推进。                                                   |
| **场景 A / 场景 B**        | 侧链角色实体化的时点分流：角色尚未出现 → 节点全量行为化进请求描述（A）；已出现 → 只给表层投影（name/gist/involvedNpcs/thread，无 motive 无连线，拿不准走 B）。            |
| **揭示**（revealed）       | 正文明确向玩家呈现过节点事实后由 post 单向置位；「活跃/已回收」都不等于玩家知道。UI 另有会话级临时 peek（不落库）。                                                       |
| **收口**（commit）         | 成功回合在 `advanceTurn` 之前由 `commitPlotThreadTurn` 把 pre 声明 + post 结算一次性窄写入袋，`lastCommittedTurn` 幂等防重。失败/取消整体丢弃、不消费冷却。               |

## EJS 世界书求值契约（工坊 Phase 2）

| 术语                     | 含义                                                                                                                                                      |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **stats 面**             | EJS 可见的**只读契约**：纯代码推导的角色资源、等级/层级/经验、五维属性、命运点数与游戏时间。写入不会提交游戏状态，创作者不得依赖 mutation 行为。          |
| **叙事变量空间**（vars） | AI 与 EJS **共写**的自由形状变量树，游戏叙事状态（事件阶段、信号、EJS 自有簿记等）的唯一居所。形状不受约束。                                              |
| **AI 赢仲裁**            | 同一回合内 AI 与 EJS 写了叙事变量空间的同一路径时，AI 的更新覆盖 EJS 的更新（AI 基于本回合正文，更新鲜）。                                                |
| **装配 pass**            | 为单个 Agent 组装一次提示的过程。EJS 求值的状态域：pass 内条目按序共享变量草稿，pass 之间互不可见。                                                       |
| **提交权**               | Agent 级授权：持权 Agent 的装配 pass 中 EJS 产生的变量改动才会在回合结算时持久化；无权 pass 的改动仅在当次 pass 内有效。默认仅主叙事 Agent（story）持权。 |
| **静态区 / 动态区**      | 世界书注入文本的两段。静态区=输出字节逐回合稳定的条目，排在前（保提示缓存前缀）；动态区=含任何动态特征（EJS/随机宏/变量宏）的条目，沉到尾部。             |
| **条目沉底**             | 把动态区条目移到世界书注入尾部的缓存保护手段。两区内部各自保持原有排序；只有动态条目会执行代码，因此沉底不改变求值语义。                                  |
| **别名层**               | 上游（MVU/酒馆助手）函数名到本引擎两轴契约（stats 面 + 叙事变量空间）的薄映射。只为存量语料存在；新内容应直接使用两轴。                                   |
| **条目级回退**           | 某条目 EJS 编译或执行失败时，该条目以原文注入（等同引入求值前的现状），不影响其它条目。零回归兜底。                                                       |

## 远程素材

| 术语                      | 含义                                                                                                                                          |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| **远程素材声明**          | 作者在自己已有的载体里写下的一行「谁 / 哪一类 / 哪个变体 / 从哪下」。它只是地址，不是素材本身。                                               |
| **素材目录**（catalogue） | 从本地载体扫出来的全部远程素材声明的归一化清单。目录的采集面 **100% 在本地**，不存在第三方索引服务。                                          |
| **双载体**                | 声明的两个来源：世界书条目正文里按上游 char-info 约定**静态抽取**的角色档（绝不执行 EJS），与内容包的第 14 分节。形状不同，归一后同一张目录。 |
| **镜像同步**              | 让本地素材库向素材目录看齐的动作：缺的下、变的换、同的留、目录里没有的（且属远程所有的）删。目标是镜像目录，不是单向累加。                    |
| **增量判定**              | 决定「重下还是留着」的依据：同一槽位上已有远程行且**地址相同**即判保留、零网络；地址变了才换字节。                                            |
| **素材哈希**              | 落地字节的内容指纹，与手工导入的素材共用同一套算法，用于素材行自身的同一性；它不是增量判定的键。                                              |
| **远程所有 / 玩家所有**   | 一条素材行的归属。玩家一旦动过（改名/提主图/取景/删除）该行即转为玩家所有，镜像同步从此不再碰它。                                             |
| **墓碑**                  | 玩家删掉某个远程槽位后留下的标记，使被删的图不会在下次同步时自己回来。                                                                        |

## API 请求管理

| 术语         | 含义                                                                                                                   |
| ------------ | ---------------------------------------------------------------------------------------------------------------------- |
| **API 凭据** | 归一化上游基础地址与 API Key 的组合；Agent、模型、存档与请求类型不同，只要使用同一组合，就属于同一份凭据。             |
| **RPM 策略** | 玩家为一份 API 凭据设定的每分钟真实请求发送上限；缺席表示不限，不等同于服务商账户实际拥有的额度。                      |
| **等待请求** | 已提出发送意图、因对应 API 凭据达到 RPM 策略而尚未发往上游的请求；到达恢复时间会自动继续，排队期间取消不消耗请求名额。 |

## 存档与时间线

| 术语           | 含义                                                                                             |
| -------------- | ------------------------------------------------------------------------------------------------ |
| **时间线恢复** | 将当前存档返回到指定快照代表的分支点，并舍弃其后的分支，使玩家可见状态与后续叙事从该点一致延续。 |
