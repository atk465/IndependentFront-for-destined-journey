# 《命定之诗独立前端》综合应用审查报告（2026-08-29）

> 审查日期：2026-08-29
>
> 审查基线：`master` @ `7e2cf93a61824f4ed2fdf5da8eedcd5641b2c988`
>
> 审查对象：公开仓应用、开发态本地 BFF、构建与测试体系、产品文档及 GitHub 治理状态
>
> 审查结论：**不建议面向一般用户发布；可继续作为面向开发者与知情测试者的技术 Alpha**

## 修复状态（截至 2026-08-29）

> 本表是本报告的唯一状态闭环。后续修复只更新状态、提交与验证证据；第 7–10 章保留
> `7e2cf93` 时点的原始分析。修复与产品裁定在状态及对应正文中追加记录，不删除原始证据。

| ID        | 优先级 | 状态               | 长期跟踪或证据位置                                   |
| --------- | ------ | ------------------ | ---------------------------------------------------- |
| CRIT-01   | P0     | 已修               | `docs/known-issue.md`；§7.1、§13.3                   |
| CRIT-02   | —      | 已撤销（产品裁定） | `docs/known-issue.md` UI-01；§7.2                    |
| SEC-01    | P1     | 已修               | `docs/known-issue.md`；§8.1、§13.3                   |
| SEC-02    | P1     | 未修               | `docs/known-issue.md`；§8.2                          |
| ONB-01    | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.3                          |
| DATA-01   | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.4                          |
| LIFE-01   | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.5                          |
| LIFE-02   | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.6                          |
| EFFECT-01 | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.7                          |
| STATE-01  | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.8                          |
| REL-01    | P1     | 未修               | `TODO.md`“正式发布打包”；§8.9                        |
| A11Y-01   | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.10                         |
| RESP-01   | P1     | 未修               | `docs/known-issue.md` UI-02、`TODO.md` 移动端；§8.11 |
| BUILD-01  | —      | 已撤销（产品裁定） | `docs/known-issue.md`；§8.12                         |
| DEV-01    | P1     | 已修（2026-09-05） | `docs/known-issue.md`；§8.13                         |
| TOOL-01   | P1     | 已修               | `docs/known-issue.md`；§8.14、§13.3                  |
| GOV-01    | —      | 已撤销（产品裁定） | `docs/known-issue.md`；§8.15                         |
| PRIV-01   | P2     | 未修               | §9.1                                                 |
| TRUST-01  | P2     | 未修               | §9.1                                                 |
| COST-01   | P2     | 未修               | §9.1                                                 |
| TIME-01   | P2     | 未修               | §9.1                                                 |
| MODE-01   | P2     | 已修               | §9.2、§13.3                                          |
| ONB-02    | P2     | 已修               | §9.2、§13.3                                          |
| ONB-03    | P2     | 未修               | §9.2                                                 |
| ONB-04    | P2     | 未修               | §9.2                                                 |
| TRUST-02  | —      | 已撤销（产品裁定） | §9.2                                                 |
| PRIV-02   | P2     | 未修               | §9.2                                                 |
| UX-01     | P2     | 未修               | §9.2                                                 |
| UX-02     | P2     | 未修               | §9.2                                                 |
| PERF-02   | P2     | 未修               | §9.3                                                 |
| TEST-01   | P2     | 未修               | §9.3                                                 |
| PLAT-01   | P2     | 未修               | §9.3                                                 |
| DOC-01    | P2     | 未修               | §9.3                                                 |
| IMPORT-01 | P3     | 未修               | §10                                                  |
| PRIV-03   | P3     | 未修               | §10                                                  |
| DEP-01    | P3     | 未修               | §10                                                  |
| DIAG-01   | P3     | 未修               | §10                                                  |
| IFACE-01  | P3     | 未修               | §10                                                  |
| PERF-01   | P3     | 未修               | §10                                                  |
| PERF-03   | P3     | 未修               | §10                                                  |
| MAINT-02  | P3     | 未修               | §10                                                  |
| UX-03     | P3     | 未修               | §10                                                  |

## 1. 执行摘要

本项目已经形成清晰、独特且完成度较高的叙事 RPG 产品形态。首页、角色创建、游戏主界面、设置、存档管理与创意工坊之间具有一致的视觉语言；引擎侧具备多 Agent 编排、存档级提示词会话、QuickJS 隔离、快照恢复、内容包与工坊扩展等较深的系统能力。自动化测试与分层约束显著强于同类项目。

然而，当前产品尚不具备面向一般新用户发布所需的首次运行完整性、故障可解释性、可访问性和分发能力。审查最初确认的一项发布阻断级问题现已修复：

1. 剧情事件的 `triggerCondition` 可进入宿主页面的 `new Function`，从模型输出或导入备份形成可达的同源任意 JavaScript 执行汇点。

CRIT-02 的原始 P0 分类已于 2026-08-29 根据产品裁定撤销。本项目是单人游戏，创角点数与资格不承担多人公平性、服务器权威或共享经济边界；玩家主动绕过限制只改变自己的开局难度，属于允许的玩家自主选择，不构成缺陷或发布阻断项。

P0 关闭后，首次运行路径仍存在一项结构性矛盾：应用将“新建存档”作为首要行动，但未在进入八步角色创建前验证内容包、API 端点、模型与 Agent 映射是否就绪。用户可以完成全部创建流程，进入游戏后才因模型未配置而收到缺乏具体原因和修复入口的通用失败提示。

本次审查共登记 42 项发现；截至 2026-08-29，33 项未闭环、5 项已修复、4 项经产品裁定撤销：

- **0 项未闭环 P0；1 项 P0 已修复：** CRIT-01 的宿主域代码执行汇点已移除；
- **11 项未闭环 P1；2 项 P1 已修复；2 项原 P1 已撤销；**
- **13 项未闭环 P2；2 项 P2 已修复；1 项原 P2 已撤销；**
- **9 项未闭环 P3；**
- **4 项已撤销：** CRIT-02、BUILD-01、GOV-01 与 TRUST-02 按产品裁定不再进入修复计划。

## 2. 评分与发布判断

评分采用 10 分制：1–3 表示不具备对外使用条件，4–6 表示具备部分能力但仍有显著缺口，7–8 表示整体稳健，9–10 表示具备成熟产品或工程基线。

| 维度               |     得分 | 结论                                                               |
| ------------------ | -------: | ------------------------------------------------------------------ |
| 产品定位与视觉识别 |     8/10 | 产品形态明确，具有稳定的游戏化视觉身份                             |
| 桌面端界面质量     |     7/10 | 完成度较高，但部分信息密度和状态表达仍需收敛                       |
| 新用户友好度       |     3/10 | 配置门槛高，且关键缺失在流程末端才暴露                             |
| 核心流程可靠性     |     4/10 | 主体可运行，但存档、回合和效果生命周期存在承重竞态                 |
| 架构与可维护性     |     6/10 | 深模块与机器闸门较强，所有权和原子边界仍不统一                     |
| 安全与隐私         |     3/10 | 多处隔离设计有效，但存在一个宿主域代码执行入口和两项高风险边界     |
| 可访问性           |     4/10 | 有全局焦点环和减动效基线，但关键自定义控件未满足既定契约           |
| 响应式与移动端     |     2/10 | 设置页部分可用，首页与游戏主界面在 390px 下不可用                  |
| 性能与构建产物     |     3/10 | 首包偏重，且本地忽略素材可静默污染构建产物                         |
| 自动化质量         |     9/10 | 测试规模、类型闸门、Lint、格式化和分层约束均较强                   |
| 分发与运维         |     3/10 | 当前仅支持源码开发态运行，无独立安装包或生产 BFF 入口              |
| 文档与贡献者体验   |     5/10 | 文档丰富且诚实，但入口、版本声明与许可文件仍不完整                 |
| 社区内容信任与安全 |     4/10 | 有未审核提示和 iframe 隔离，但能力披露、远程访问和成人内容控制不足 |
| **总体发布准备度** | **5/10** | **技术 Alpha；不建议面向一般用户发布**                             |

## 3. 审查范围与方法

### 3.1 覆盖范围

本次审查覆盖以下方面：

- 首页、八步角色创建、首次进入游戏、API 设置、Agent 设置、存档管理、创意工坊、关于页；
- 1280×720 桌面视口与 390×844 窄视口；
- DOM 可访问性树、焦点行为、自定义控件语义和减动效基础设施；
- 存档加载、回合取消、状态提交、效果接线、剧情事件、备份导入导出；
- 本地 Hono BFF、跨源策略、上游代理、内容写回、远程素材与工坊美化 iframe；
- 构建产物、音频污染路径、依赖健康、Node 版本约束、启动器行为；
- 类型检查、测试、格式化、Lint、Knip 棘轮、分层闸门与 GitHub 分支保护状态；
- README、PRD、架构文档、TODO、已知缺陷与贡献者入口。

### 3.2 证据方法

审查采用以下证据来源：

1. 当前基线源码与文档的逐路径回溯；
2. 当前运行实例的首次使用与返回用户流程走查；
3. 桌面与窄视口截图、可访问性树和控制台错误；
4. 相关单元测试、集成测试和分层闸门；
5. 实际构建产物大小与内容统计；
6. 当前 GitHub 仓库分支保护状态的只读查询；
7. `npm audit`、依赖树与 tracked-secret 模式扫描。

### 3.3 排除项

本次未执行以下验收，因此不得将相应结论外推为已验证：

- 真实付费模型的一整轮叙事生成及长期游玩；
- 私有内容仓全部世界书、脚本和素材的逐项安全审查；
- 真实手机、屏幕阅读器与 macOS 硬件验证；
- 独立安装包、正式生产托管与反向代理安全头；
- 全部 10 个主题的逐主题视觉、对比度与交互截图验收；
- 外部工坊服务、模型提供商与 `wsrv.nl` 的服务端实现。

## 4. 优先级定义

- **P0：** 常规可达路径可造成同源代码执行、承重状态约束破坏或不可接受的数据/凭据风险，应阻断发布；
- **P1：** 主流程、数据一致性、可访问性、安全边界、分发或工程治理存在显著缺陷，应在发布候选版前完成；
- **P2：** 不必然阻断主流程，但会明显影响理解、信任、成本、性能、可维护性或回归安全；
- **P3：** 影响有界，适合在主风险收口后进入常规清理或抛光。

## 5. 长期跟踪边界

本报告开头的状态表是唯一报告内闭环。未修 P0/P1 同步记录于
[`docs/known-issue.md`](../known-issue.md) 的“2026-08-29 综合审查未闭环 P0/P1”章节；正式发布、
macOS、响应式和移动端等交付工作继续引用既有 [`TODO.md`](../../TODO.md) 条目，不在两个登记处重复。

修复任一条目时，应在同一提交中回写报告状态表与其长期跟踪位置，保留第 7–10 章分析正文作为
`7e2cf93` 时点的审查快照。

## 6. 已确认的优势

### 6.1 产品与视觉

- 首页、创建页和游戏页具有统一且可辨识的暗色叙事 RPG 语言，并非通用后台或聊天壳；
- 侧边工具栏保留文字标签，核心功能不依赖纯图标猜测；
- 装备选择同时提供类型、来源、品质、搜索和已选汇总，是当前最成熟的复杂目录交互；
- 存档管理具有正确的 dialog 语义、详情与操作分离、图标操作的可访问名称；
- 内容包状态、API 空态、图片生成配额与失败降级具有较清楚的产品表达。

### 6.2 工程与数据安全

- 当前自动化证据为 9,273 项测试通过，覆盖 361 个测试文件，另有 8 项跳过；
- 三组类型检查、Prettier、ESLint 零 warning、Knip ratchet 与生产构建均有通过证据；
- `tests/layering-gate.test.ts` 以 17 项测试覆盖静态 import、动态 import 与字符串路径绕过；
- Prompt session 按 `(saveId, agentId)` 所有，并以 session/revision 拒绝过期完成；
- 同存档写队列按 saveId FIFO 串行，快照恢复、删档与单存档导入使用多表事务；
- 整库导入在变更前校验版本与形状，保留内存预备份并在失败时尝试回滚；
- 单存档导入重发 ID，且不触碰全局表；
- 生产 EJS 与效果脚本使用带时间、内存、栈和微任务预算的 QuickJS，装载失败时先进入 fail-closed；
- 富文本美化运行于 opaque-origin iframe，阻止其直接访问父应用 DOM 与存储；模型生成卡片使用更严格的禁脚本、禁网络策略；
- tracked-secret 与敏感文件名扫描未发现已提交凭据；仓库未发现分析、遥测或错误上报 SDK。

### 6.3 文档与治理文化

- 仓库主动记录尚未完成的移动端、打包、macOS 与真机验收，不将计划状态包装为已交付；
- 内容包、远程素材、图像生成、API 限流、Prompt Delta 与时间线恢复均有较完整的设计文档；
- 编码不变量、内容分离、分层方向与死代码增长已转化为机器闸门，而非仅依赖人工约定。

## 7. 发布阻断项（P0）与撤销项

### 7.1 CRIT-01：剧情触发条件可执行宿主域 JavaScript

- **触发源：** 被提示注入影响的 `plot_post_check` 输出可创建 `newChildEvents[].triggerCondition`；导入的单存档备份也可携带同字段；
- **可达路径：** `game-pipeline.ts:1847-1854` 将原始后检输出交给 `postCheckPlot`，`plot-engine.ts:276-323` 原样持久化子事件条件；待处理事件及其条件会再次提供给 Plot Agent（`agent-templates.ts:383-400`），当模型选择相应事件后，`preCheckPlot` 调用 `evaluateCondition`；
- **根因：** `plot-engine.ts:27-56` 将条件字符串插入 `new Function`。源码注释称“限制可用全局”，实现中不存在任何全局限制；
- **影响：** 代码在应用同源环境中执行，可读取 IndexedDB 中的存档与 API Key、localStorage 中的设置与工坊令牌、调用同源 BFF、修改游戏数据或向外部发送数据；
- **备份入口：** `session-backup.ts:648-681,695-751` 只校验顶层结构，并通过对象展开保留剧情事件其余属性；
- **验证边界：** 源码路径确认该代码执行汇点可达；本次审查未执行攻击载荷，不将其表述为已完成的动态利用演示；
- **修复要求：** 使用不可执行的条件表达式解析器，或复用已有 QuickJS 隔离后端；模型输出与导入数据在持久化前均须经过同一 schema/语法白名单；补模型输出、导入备份和宿主全局不可达三类回归测试。

> **2026-08-29 修复记录：** 进一步核对生产契约后确认，`triggerCondition` 从提示词生产端到
> `PlotEvent` 类型定义均为自然语言语义提示，不是表达式语言。因此修复没有引入另一套求值器，
> 而是删除 `plot-engine` 的宿主域 `new Function` 及其重复条件否决；`plot_pre_check` 的事件标题选择
> 现为唯一语义裁决，Code 侧只验证标题存在且状态为 `pending`。回归测试覆盖模型选择、恶意外观文本、
> 单存档备份导入与宿主全局不发生变化。原始“表达式解析器或 QuickJS”建议由该契约核对结果取代。

### 7.2 CRIT-02（已撤销）：禁用创建选择可绕过资格与点数约束

> **2026-08-29 产品裁定：** 撤销 P0 与缺陷分类。本项目为单人游戏，玩家可以自行决定开局难度；主动绕过创角限制不影响其他玩家、服务器权威或共享经济，因此不要求修复。

- **原始触发路径：** 玩家点击被视觉标记为禁用的装备、物品或技能卡片内部“选择”按钮；
- **实现事实：** `SelectableCard.vue:18` 附近的内层按钮仍可发出选择事件；`create-store.ts:695` 起的 mutation 不调用 `canSelect`；最终 `startJourney()` 也没有重新执行完整创建约束；
- **可观察结果：** 可产生负点数、种族或身份资格不满足的持久存档；
- **处理结论：** 上述结果仅作用于玩家自己的单人存档，按产品裁定属于允许行为；不进入修复计划，除非未来产品边界发生变化并获得重新授权。

## 8. 高优先级问题（P1）

### 8.1 SEC-01：整库备份导出的 JSON 包含未脱敏 API Key

`ApiEndpoint.apiKey` 的接口类型为字符串（`types.ts:335-354`）。`FullBackup` 明确包含 `apiEndpoints`（`database.ts:711-750`），`exportAllData()` 原样读取该表（`:772-845`），设置页再直接 `JSON.stringify` 为可读下载文件（`DataSection.vue:367-377`），过程没有脱敏。本结论只针对导出表示，不对浏览器存储的物理加密状态作未经验证的判断。

设置页说明详细列出音频、素材和内容包不随备份导出，却没有说明凭据会进入备份（`DataSection.vue:451-483`）。与此相对，localStorage 持久化会主动清空 Key（`settings-store.ts:126-136`），说明整库导出绕过了已有的凭据边界。

**修复要求：** 默认从备份中排除 API Key；如需支持凭据迁移，应采用独立、显式确认且清楚标记敏感性的导出流程。

> **2026-08-29 修复记录：** `apiEndpoints` 与可能残留旧凭据的 `settings` 已改为设备本地数据；
> 新 FullBackup 不再导出这两张表，导入新旧备份也不会清空、恢复或覆盖本机对应行。设置页与首页的
> 导出、导入说明均明确披露：API 端点和 Key 不进入备份，换设备后必须重新配置，导入不会改变本机
> 凭据。回归测试同时覆盖新导出不含密钥、旧备份携带密钥及空数组均无法覆盖本机凭据。

### 8.2 SEC-02：本地 BFF 对普通 Web Origin 开放条件性高风险能力

`server/app.ts:74-103` 仅拒绝 `Origin: null`，随后为其他来源返回 `Access-Control-Allow-Origin: *`，并允许调用方提供 `Authorization`、`api-key` 与 `X-Target-Base-URL`。`proxy.ts:17-30,47-102` 允许调用方指定上游，且刻意保留 localhost 与私有 IP，以支持本地模型。BFF 不会自动附加应用已存储的 Key；风险来自跨站调用方自带凭据或访问无需凭据的本地能力。

固定路由仍可调用本地 chat/models/embeddings、NovelAI 与 ComfyUI 的 prompt/history/view/queue/interrupt；内容路由在配置 `POEM_CONTENT_DIR` 后可将未设大小上限的文本请求体写入 `.json` 文件（`content.ts:43-87`）。Windows 启动器发现相邻私有内容仓时会自动启用该目录（`dev.bat:23-42`）。

**限定因素：** Vite 当前默认仅监听 loopback，跨站浏览器利用是否成立取决于 Private Network Access 等浏览器限制；浏览器扩展与本地进程不受同等限制。因此本项按条件性高风险登记，而非声明所有浏览器均可直接利用。

**修复要求：** 将 BFF 限定为可信 Origin，并引入不可由外部站点获得的会话能力；内容写回需要独立授权、请求体上限和明确的开发模式边界。

### 8.3 ONB-01：首次运行缺少就绪门，失败暴露过晚

README 要求配置三个 API 并为 Agent 绑定模型，但首页在未验证内容包、端点、模型和 Agent 映射时仍将“新建存档”作为主行动。用户可以完成八步创建；首次进入游戏后，各 Agent 因未配置模型失败，界面只显示“世界的回应在此中断，可以再次尝试”，具体原因仅写入 console（`game-pipeline.ts:478-512,756-774`）。

API 空态本身具有较清楚的说明，但用户必须在失败后主动找到设置页；Agent 设置呈现 12 个警告状态，没有一键应用推荐映射或配置完成度。

**修复要求：** 在角色创建之前加入首次运行清单：内容包状态、API 必填校验、连接测试、推荐 Agent 映射、Embedding 可选性、预估调用数量与成本。失败卡片应显示具体原因并直接跳转到对应设置分区。

### 8.4 DATA-01：新旅程创建非 single-flight、非事务且错误不可见

`CreatePage.vue:29-37` 未设置 creating/busy 状态；最终按钮只受步骤合法性控制；失败只写 `console.error`。`create-store.ts:1784-1852` 依次写角色、存档、profile、命运点、剧情大纲和事件，没有统一事务或回滚。

双击可启动两次创建；中途失败可留下孤儿角色或半份存档。

**修复要求：** 提供 engine 级 `createJourney` 事务、UI single-flight 锁，以及可见且可重试的错误状态。

### 8.5 LIFE-01：活跃存档双所有者与异步装载竞态

`ui.activeSaveId` 与 `game.activeSaveId` 是两个独立 ref。导航先修改前者，`game-store.loadSave` 经过多个 await 后无 generation、requestedSaveId 或 disposed 检查地提交后者及整份投影（`ui-store.ts:34-50`；`game-store.ts:981-988,1033-1078,1113-1148`）。

当用户打开慢加载存档 A 后迅速返回并打开 B，A 的异步完成可能覆盖 B 的角色投影，或让已卸载页面继续构造 pipeline、消费开场提示。

**修复要求：** 建立唯一 `SaveSession` 所有者；每次装载捕获 requestedSaveId 与 generation，并在每个 await 后验证；`loadSave`、`restoreMessages`、`refreshFromDb` 接收显式 saveId 并采用 last-load-wins 提交栅栏。

### 8.6 LIFE-02：Stop 后旧回合与新回合共享可变状态

`GamePipeline` 将 `currentContext`、`pendingPlotTasks`、`pendingAudioMarker`、`lastStoryMessage` 与 `chainData` 保存在共享实例上（`game-pipeline.ts:232-282`）。Stop 会提前允许新输入，而旧回合可能仍在等待不响应 abort 的剧情或 embedding 任务。新回合覆盖字段后，旧 finally 可读取或清理新回合状态（`:387-422,514-556,638-642`）。

**修复要求：** 每次运行创建独立 `RunSession`，共享 pipeline 只保留 currentRun 指针；或在旧 run 完整收尾前不解锁输入。

### 8.7 EFFECT-01：效果接线未与权威投影做增删对账

`effect-wiring.ts:56-83,132-164,187-217` 对 owner 做幂等添加，但不会删除权威角色投影中已消失的 owner。除显式 `unequip_item` 与时间线恢复外，替换装备、删除或转移物品、更新脚本、删除或更新技能、删除角色及删档均缺少统一拆线。

结果是已移除的装备或技能仍可能响应事件，新脚本也可能因旧 owner 已存在而永远不注册。

**修复要求：** 提供唯一 `reconcileEffectWiring(saveId, characters)`，按权威 owner 集合做 diff；在装载、角色提交、页面/会话销毁及删档时统一调用。

### 8.8 STATE-01：StateManager 原子契约与部分成功实现冲突

`state-manager.ts:1-12,184-199` 将公开契约描述为全部成功或全部回滚，但实际执行在单条 patch 失败后继续后续 patch，并无条件 flush 成功部分（`:350-448,586-595`）。测试明确固定了部分成功行为（`state-manager.test.ts:3456-3480`）。

制作、战斗与重铸调用方仍把提交当作原子领域命令；材料扣除失败时，HP、经验、FP 或产物信息仍可能部分生效。

**修复要求：** 拆分显式 best-effort AI patch API 与 atomic domain-command API；制作、战斗、重铸和建档应完整预验证，并在同一 Dexie 事务与 save lock 内提交。

### 8.9 REL-01：无可分发安装包或生产 BFF 入口

README 已明确当前为开发版且无独立安装包（`README.md:19-31`）。`docs/ARCHITECTURE.md:300-304` 说明 `buildHonoApp()` 仅挂载于 Vite dev/preview，没有独立进程入口或打包产物。

当前新用户必须安装 Node、下载源码、安装依赖并运行开发服务器，无法按一般桌面应用或受支持静态服务使用。

**修复要求：** 在 `TODO.md`“正式发布打包”中裁定桌面壳、静态站+BFF 或双形态方案，并定义版本、更新、内容包、凭据与首次启动策略。

### 8.10 A11Y-01：既定键盘与 WCAG 契约未兑现

`PRODUCT.md:49-54` 要求 WCAG AA 与键盘可用，但共享 `AppModal.vue:76-108` 缺少 `role="dialog"`、`aria-modal`、标题关联、初始焦点、焦点陷阱和焦点归还。Toast 无 live region（`ToastContainer.vue:21`）；首页存档行、难度卡片和背景卡片使用可点击 div，缺少键盘等价行为（`HomePage.vue:645`、`CreateStepDifficulty.vue:23`、`BackgroundList.vue:110`）。

**修复要求：** 先修共享基元，再修必选流程控件；加入真实键盘遍历与 axe/ARIA 回归测试。

### 8.11 RESP-01：窄视口下首页和游戏主界面不可用

390×844 实测中，内容状态横幅逐字换行并覆盖首页标题与主行动。游戏页仍保持固定桌面画布；`GamePage.vue:488` 的 `min-width: 900px` 导致页面水平裁切，仅显示局部内容。设置页可堆叠，但导航需要横向发现。

该问题与 `docs/known-issue.md` UI-02 及 `TODO.md`“多分辨率 / 多宽高比适配”“移动端支持”一致。

**修复要求：** 在支持完成前明确声明最低桌面宽度；正式修复需建立紧凑桌面态和移动/窄屏状态，而非继续压缩三栏桌面布局。

### 8.12 BUILD-01：本地忽略音频可静默进入构建产物

> **2026-08-29 产品裁定：** 本项撤销，不进入当前修复计划；原始证据作为审查历史保留。

Vite 会复制 `public/` 下的忽略文件（`.gitignore:73`、`vite.config.ts:156`）。本机当前工作副本的成功构建包含 57 个 MP3，共 265.98 MiB，使 `dist-ui` 达 309.66 MiB；运行时 manifest 仍为空。`public/audio/README.md:81` 已说明部分本地曲目未获分发授权。由于这些文件被 Git 忽略，干净克隆与 CI 不包含同一音频集合；风险属于主机状态污染发布产物，而非可复现的仓库基线产物。

**修复要求：** 发布构建采用显式 allowlist，而不是依赖人工清理；CI 对产物路径、数量、体积与许可清单做断言。

### 8.13 DEV-01：开发启动器会终止非项目进程

`dev.bat:45-63` 对 5173–5179 的全部监听进程执行 `taskkill /F`，`dev.sh:43-55` 使用 `kill -9`，均未验证 PID 是否属于本仓库。

**修复要求：** 只清理由本项目记录的 PID；其他端口冲突应报告并退出，或要求用户显式确认清理。

### 8.14 TOOL-01：Node 版本声明低于当前依赖要求

README 声明 Node 18+（`README.md:31`），当前 Vitest 要求 Node 20+，jsdom 要求 20.19+（`package-lock.json:6298,4615`），而 `package.json` 无 `engines`、`packageManager` 与版本文件。

**修复要求：** 统一为 Node 20.19+，增加版本与包管理器约束，并将贡献者安装命令改为可复现的 `npm ci`。

> **2026-08-29 修复记录：** `package.json` 已声明依赖实际支持的 Node 范围与 npm 10.9.3；
> `.nvmrc` 固定为仍受上游支持的 Node 22.23.2，Node 20.19 仅保留为依赖兼容下限。锁文件同步记录
> 引擎范围，README 改为准确版本说明与 `npm ci` 安装流程。

### 8.15 GOV-01：`master` 无分支保护和必需检查

> **2026-08-29 产品裁定：** 本项撤销，不进入当前修复计划；原始证据作为审查历史保留。

2026-08-29 的 GitHub 只读查询返回 `protected: false`，required checks 与 enforcement 均为空。仓库虽有完整 CI，但远端未强制代码合并前通过 `types`、`quality` 与 `test`。仓库明确允许经过本地格式检查并在推送后验证 CI 的纯 Markdown 直推；本项不否定该有意工作流，而是指出代码与配置变更缺少远端强制门禁。

**修复要求：** 为 `master` 启用分支保护并要求三项 CI job；限制绕过权限并保留紧急回滚流程。

## 9. 中优先级问题（P2）

### 9.1 安全、隐私与成本

| ID       | 问题                                     | 当前证据与影响                                                                                                                                                                                                                                     | 建议                                                                                    |
| -------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| PRIV-01  | 新增或变更的远程素材默认自动同步         | `settings-store.ts:310-316` 默认开启；未变化的已缓存素材不会发起网络请求，但素材首次声明或 URL 变化后的同步会联系内容作者指定主机，暴露 IP 与同步时机；直接请求失败还会把完整 URL 交给 `wsrv.nl`，字面地址过滤也不覆盖解析后或重绑定到私网的主机名 | 安装/首次同步前展示主机与能力清单，增加显式同意；验证解析后的目标地址并谨慎处理代理回退 |
| TRUST-01 | 工坊美化规则拥有脚本与外网能力但披露不足 | 工坊 regex replacement 可含 `<script>`/`<style>`；allow 策略允许脚本、eval、HTTP(S)/WS，网络可达性仍受浏览器 CORS/PNA 影响；opaque iframe 阻止直接访问父应用 DOM/存储，但不等于规则本身无网络或叙事数据披露能力                                    | 安装时披露脚本/网络能力；默认禁网或按项目授权                                           |
| COST-01  | 多 Agent 调用无硬性费用上限              | 普通回合最多调度 7 个角色；65,536 token 与 3 次重试是回退值，无 RPM 策略时不限速。图像生成相对安全，默认手动且已有配额                                                                                                                             | 提供单回合调用预估、默认安全 RPM、token/日费用上限                                      |
| TIME-01  | 声明的 120 秒总超时未实际执行            | `Pipeline.timeout` 无生产消费点；流式只限制首字节；300 秒超时与 `maxRetries=3` 意味着单个 provider round 最坏约 20 分钟并叠加退避，工具轮次还可继续延长                                                                                            | 建立 run 级 deadline，并合并用户取消信号传递到主链、侧链与退避                          |

### 9.2 首次使用、信息架构与信任

| ID       | 问题                               | 当前证据与影响                                                                                                                        | 建议                                                               |
| -------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| MODE-01  | 零命运点时经验档位静默丢失         | `experienceMode` 仅在 `destinyPoints > 0` 时写入 profile（`create-store.ts:1822-1832`）；选择简单经验但不兑换命运点时会按 normal 运行 | 将经验档位从命运点兑换条件中分离并无条件持久化；增加零兑换回归测试 |
| ONB-02   | 可在 25 点基础属性全部未分配时继续 | `create-store.ts:112-123` 只要求余额不为负；新用户可创建全零基础属性而无明确风险提示                                                  | 提供推荐分配、自动分配或提交前警告；若允许保留点数需说明去向       |
| ONB-03   | 命定核心与角色目录缺少发现工具     | 大量条目直接平铺，缺搜索、分组、推荐或全选策略；部分角色卡出现作者态/实现态文本                                                       | 复用装备页的搜索、类别和选择汇总结构；清理展示文本                 |
| ONB-04   | 剧情模式关闭时仍显示生成 CTA       | `CreateStepPlot.vue` 只隐藏雷点，生成按钮无条件存在；用户难以判断该操作是否改变模式或产生费用                                         | 关闭模式下隐藏/禁用并说明；或点击生成时明确切换模式与费用          |
| TRUST-02 | 工坊缺少成人内容与能力筛选         | 实测列表出现 NSFW/成熟内容标记，当前可见筛选仅有系统、扩展、角色、事件                                                                | 增加成人内容开关、年龄提示、默认隐藏策略与举报/审核状态            |
| PRIV-02  | 关于页缺少数据流与隐私说明         | 未说明 API Key 存储、Prompt 发送对象、调试记录持久化、远程素材和工坊脚本能力                                                          | 增加本地数据、第三方服务、社区内容和费用的集中说明                 |
| UX-01    | 浏览器模式“退出”通常无效           | `HomePage.vue:166` 调用 `window.close()`，普通浏览器标签通常拒绝                                                                      | 按宿主能力显示；普通浏览器中隐藏或提供明确反馈                     |
| UX-02    | API 配置反馈语言不一致             | `ApiSection.vue:219` 一带混用中文、`ok`、`fail`、`API added/updated` 与上游原文                                                       | 统一为可操作的中文主信息，上游原文放详情                           |

> **2026-08-29 修复记录（MODE-01）：** 新建存档现无条件创建 SaveProfile 并持久化用户选择的
> `experienceMode`；命运点为正时由 `addFP` 一次性保存，零命运点时由 `updateProfile` 保存。
>
> **2026-08-29 修复记录（ONB-02）：** 按产品裁定采用强制分配：基础属性点与等级带来的额外属性点
> 必须全部分配，即两类余额均为零，下一步才可用。确认页与 `startJourney` 复用同一不变量；晚加载
> 未分配属性的旧预设会返回基础属性页。回归测试覆盖 0/25、24/25、25/25、额外属性点未分配、
> `nextStep` 阻止与放行以及最终建档拦截。
>
> **2026-08-29 产品裁定（TRUST-02）：** 本项撤销，不进入当前修复计划；原始证据作为审查历史保留。

### 9.3 性能、测试与贡献者体验

| ID      | 问题                       | 当前证据与影响                                                                             | 建议                                                             |
| ------- | -------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- |
| PERF-02 | 首包较重且无预算           | 构建产生约 512 KiB、620 KiB、735 KiB 的 JS chunk 与约 579 KiB 初始 CSS；主题和字体集中导入 | 路由/主题拆分，建立首屏与总产物预算，并用真实启动指标验收        |
| TEST-01 | 缺少真实浏览器关键路径测试 | Vitest 以 Node 为默认，仅个别 jsdom；无 Playwright/Cypress、axe、Lighthouse 或覆盖率门槛   | 建立小型 E2E：首次运行、创角、首回合、弹窗焦点、备份脱敏与 390px |
| PLAT-01 | 跨平台声明超出当前证据     | CI 仅 Ubuntu/Node 20；TODO 明确 macOS 未经真机验证                                         | 增加启动器 smoke test 与真实 macOS 验收，再扩大支持声明          |
| DOC-01  | 贡献者与许可入口不完整     | 无 `CONTRIBUTING.md`、Node pin、环境示例与根 LICENSE；README/package 声明 MIT              | 补人类贡献者入口、准确架构入口、版本约束与实际许可正文           |

## 10. 低优先级问题（P3）

| ID        | 问题                                            | 影响与边界                                                                                                                                           |
| --------- | ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| IMPORT-01 | JSON 导入与工坊响应无体积/数量上限              | `File.text()` + `JSON.parse()` 在主线程处理整份数据，可造成页面卡死或内存耗尽；ZIP 资产导入已有更强的流式上限范例                                    |
| PRIV-03   | 开发者模式关闭时仍持久化完整 Prompt/响应/推理   | 数据仅本地保存、限 10 轮且不进入 FullBackup，但玩家未被告知额外持久化范围                                                                            |
| DEP-01    | 依赖图存在 high advisory 影响项且 CI 无安全扫描 | `npm audit` 报告生产图 7 个、全量图 12 个 high 级受影响包条目，主要扇出自同一 `nanoid 3.3.17` advisory；未发现应用源码调用其有风险的自定义生成器路径 |
| DIAG-01   | 管线验证错误计算后被丢弃                        | 调用链失去结构化错误上下文，故障最终退化为通用失败或 console 诊断                                                                                    |
| IFACE-01  | 旧接口形状与未注册入口保留                      | 旧 `userId` 形状及未注册的 `ScriptRegistry` 增加维护者对现行契约的判断成本                                                                           |
| PERF-01   | 全局记忆写锁包含网络 embedding                  | `state-write-queue.ts:44-50` 明令锁内不含 LLM；`memory-summarizer.ts:182-228` 却在锁内 await embedding，影响仅限并发存档的记忆写入串行等待           |
| PERF-03   | 两个进度条动画 width                            | `PlotOutlinePreview.vue:218`、`ResourceBar.vue:59` 触发布局工作；应改为 transform                                                                    |
| MAINT-02  | Knip 棘轮接受 140 条存量                        | 棘轮阻止新增是优点，但长期基线仍会固化死代码与无效导出                                                                                               |
| UX-03     | 存档管理列表的长名称和次要操作发现性一般        | 长名称在窄列表中换行，部分操作依赖 hover；影响有限                                                                                                   |

## 11. 新用户流程健康度

| 步骤 | 页面或任务     | 健康度     | 结论                                                                 |
| ---: | -------------- | ---------- | -------------------------------------------------------------------- |
|    1 | 首次首页       | 混合       | 视觉完成度高且明确提示演示内容，但主行动绕过配置就绪条件             |
|    2 | 难度选择       | 混合       | 比较清楚，但卡片为鼠标专用伪控件                                     |
|    3 | 基础信息与属性 | 良好       | 信息可理解；基础与额外属性点未全部分配时无法进入下一步               |
|    4 | 命定核心       | 风险       | 世界观表达丰富，但条目过多、命名偏实现态、缺少发现工具               |
|    5 | 角色启用       | 混合       | 目录过长且部分卡片泄露实现态文本；资格限制按产品裁定可由玩家自主选择 |
|    6 | 装备选择       | 良好       | 搜索、类型、品质、已选汇总完整，是可复用范例                         |
|    7 | 背景选择       | 混合       | 内容丰富，但长列表与鼠标专用卡片增加负担                             |
|    8 | 剧情设置       | 混合       | 说明充分，但“关闭”与生成 CTA 的关系不清楚                            |
|    9 | 确认提交       | 风险       | 摘要有效，但不验证内容包、API 与 Agent 就绪状态                      |
|   10 | 首次游戏回合   | 阻断       | 缺模型时立即失败，界面不展示具体原因或设置入口                       |
|   11 | API 空态       | 良好但过晚 | 说明与推荐清楚，但通常在失败后才被发现                               |
|   12 | 添加 API       | 混合       | 字段清楚，Key 使用 password 输入；共享弹窗焦点契约不完整             |
|   13 | Agent 映射     | 阻断       | 12 个警告状态，无推荐批量应用与完成进度                              |
|   14 | 存档管理       | 良好       | 对话框语义、详情和操作分层较好                                       |
|   15 | 创意工坊       | 混合       | 搜索与排序完整，但能力和成人内容控制不足                             |
|   16 | 关于页         | 混合       | 致谢与技术栈清楚，缺隐私、数据流、费用和社区能力说明                 |
|   17 | 390px 首页     | 失败       | 内容横幅覆盖标题与主行动                                             |
|   18 | 390px 设置     | 混合       | 主体可堆叠，导航发现仍依赖横向滚动                                   |
|   19 | 390px 游戏     | 失败       | 固定桌面画布被水平裁切，核心流程不可用                               |

## 12. 建议实施顺序

1. 建立首次运行就绪门，完成内容、API、模型、Agent、费用与 smoke test 引导；
2. 收紧 BFF Origin 与能力边界；
3. 将新旅程创建改为 single-flight 事务，建立唯一 SaveSession 和独立 RunSession；
4. 对效果接线进行权威集合对账，拆分 best-effort patch 与 atomic domain command；
5. 修复共享弹窗、Toast 和必选控件的键盘/ARIA 契约；在移动端未完成前明确最低支持宽度；
6. 裁定正式分发形态，并补充真实平台验收；
7. 增加真实浏览器关键路径测试和窄视口测试；
8. 补远程请求、数据持久化和费用披露；
9. 最后处理 chunk 拆分、Knip 基线、动画属性与界面抛光。

## 13. 验证记录

### 13.1 原始审查基线（修复前）

- `npm.cmd run typecheck`：通过；
- `npm.cmd run typecheck:vue`：通过；
- `npm.cmd run typecheck:tools`：通过；
- `npm.cmd run build`：独立重跑通过；
- `npm.cmd run format:check`：通过；
- `npm.cmd run lint`：通过，0 warning；
- `npm.cmd run knip:ratchet`：通过，存量基线 140；
- `npm.cmd run test:run`：9,279 项通过，361 个测试文件，8 项跳过。

审查并行阶段的冗余构建曾在 Windows 上因已生成的 `dist-ui` 音频/图片文件被占用而出现 `EPERM unlink/copyfile`；停止相关并行活动后，独立构建通过。该现象未被归类为产品代码失败，但说明本地生成目录仍可能受 Windows 文件锁影响。

### 13.2 专项验证

- 架构与生命周期相关 5 个测试文件共 276 项通过；
- 分层闸门 17 项通过；
- 安全路径 7 个测试文件共 262 项通过；这些测试验证周边既有行为，不构成 CRIT-01 攻击载荷的动态利用测试；
- `npm audit --omit=dev`：7 个 high 级受影响包条目、0 critical；
- `npm audit` 全量：12 个 high 级受影响包条目、0 critical；两组结果主要扇出自同一 `nanoid 3.3.17` advisory；
- tracked-secret 与敏感文件名扫描：未发现匹配；
- 当前 `master` 与 `origin/master` 在审查开始时为 `0 0`；
- 审查启动的本地服务已停止，5173 端口无监听。

### 13.3 首批修复验证（2026-08-29）

- 6 个聚焦测试文件共 416 项通过，覆盖剧情条件惰性处理、跨备份恢复攻击文本、凭据排除与旧备份兼容、备份披露、难度模式持久化、创角属性点闸门及晚加载旧预设的最终建档拦截；
- 完整 `npm.cmd run gates` 通过：三组类型检查、Vite 生产构建、Prettier、ESLint（0 warning）、Knip 棘轮（140 条存量问题、无新增）以及 Vitest 全量测试均通过；
- Vitest 全量结果为 361 个测试文件通过，9,273 项通过、8 项跳过；
- 渲染走查确认创角步骤在基础属性点为 0/25 与 24/25 时禁止继续，在 25/25 时允许继续；提高等级产生额外属性点后再次禁止继续，额外点全部分配后恢复允许；
- 设置页渲染走查确认备份入口明确告知 API 端点与 Key 不进入备份、导入保留设备本地凭据、新设备需重新配置；
- 对全部变更文本执行 UTF-8 编码检查，U+FFFD 与非法控制字符计数均为 0，JSON 文件均可解析；
- 完整闸门首次在 Windows 沙箱内构建时因 `dist-ui` 生成文件无法替换而出现 `EPERM unlink`；宿主权限下独立构建及随后完整闸门均通过，因此该现象判定为沙箱文件权限问题，而非产品构建失败；
- 本轮启动的浏览器页签与 Vite 服务均已关闭。

## 14. 结论

本项目的主要矛盾不是功能数量不足，而是核心系统深度已经超过首次运行、生命周期所有权、安全披露和发布工程的成熟度。继续增加内容或子系统会扩大这些边界问题的影响面。

CRIT-01 与凭据导出问题已关闭，当前无未闭环 P0。然而，在 BFF 跨源能力、建档事务、Save/Run 生命周期、首次运行就绪门、可访问性与正式分发完成之前，仍不应将当前版本定义为面向一般用户的可发布产品。完成上述收口后，项目已有的视觉身份、测试体系、内容架构与多 Agent 能力足以支撑一个具有竞争力的桌面叙事 RPG 产品。

本报告以 `7e2cf93` 为原始审查快照；原始分析保留不改写，后续修复与产品裁定以文首状态表、正文日期注记、验证记录及 `docs/known-issue.md` 形成闭环。

> 2026-09-05 更正：ONB-01、DATA-01、LIFE-01、LIFE-02 本轮修复见 [首轮可靠性验收](2026-09-05-first-session-reliability.md)。原始分析与历史计数保留；ONB-01 的自动推荐映射和成本估算不属本轮范围。

> 2026-09-05：EFFECT-01 / STATE-01 / A11Y-01 / DEV-01 已实施；效果按权威集合对账，领域命令整批事务提交，共享弹窗与必选控件补齐键盘支持，启动器端口冲突不再终止进程。范围与验收见 [剩余四项可靠性修复](2026-09-05-remaining-reliability.md)。
