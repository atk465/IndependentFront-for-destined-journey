# Known issue: 主动技能的 on-hit 战斗效果不生效 + `资源` modifier 方向反（2026-09-11 记档）

状态：**已定位根因、暂不修**（涉及战斗语义设计，需先定「主动技能的效果该挂在谁身上」）

真机现象（`fated-poem-debug-655aa8ec-1789147265734.json`）：item_gen 产出的主动技能
`火球术`（法力燃烧：目标额外损失 400MP）、`灼热射线`（灼烧 DOT：目标每回合开始 30 点能量伤害）
在战斗里**不产生任何附加效果**。仅 `skillPower` 进伤害公式（火球术初始伤害
`int(8)×10×2.8 + 400 + weaponAtk(75) = 699`，减免前 —— 这个数字本身是对的）。

根因两端：

1. **主动技能的 modifiers / buffs / automata 根本没被收集进战斗**
   （`src/sillytavern/combat-v2-types.ts:characterToCombatParticipant`）：participant 的 `modifiers` 只收
   **已装备物品**；`automata` 只收「已装备物品 + **被动**技能」；主动技能仅摘了
   `skillPower/relevantAttribute/damageType` 进 `activeSkills`（只喂伤害公式）。
   故主动技能声明的 `<modifiers>` / `<buffs>` / `<automaton>` 全部落空。
2. **即便接通，`附加效果` / `资源` modifier 的编译方向也是反的**
   （`src/sillytavern/combat-v3/automata/compile.ts`）：`ApplyStatus` / `SpendResource` 的 `targetId`
   一律写死 `seed.owner`（施法者自己），且 `资源` 订阅 `round.open`（每回合无条件）。
   对**装备**（owner = 穿戴者）语义正确；对**主动攻击技能**应是「命中目标时对其施加」，
   直接接通会变成「自己每回合掉 400MP / 自己中灼烧」——比现在更糟。

修法方向：给主动技能的效果编译一条独立路径 —— 目标取防守方、窗口取 `check.hit` / `damage.after`，
与装备的 owner 自身订阅区分开（可能需要在 modifier 上多一个「作用对象」判别字段）。
在定下这条语义前，**不建议「顺手把主动技能也收进 participant.modifiers」**。

---

# Known issue: 地图 v1.2 结算的两条收益丢账（低危，只少给钱不多给钱）

状态：**第 1 条仍存；第 2 条已修**（2026-09-05 修复 F08 可恢复收益，见下方 📌 注记；原裁定见
2026-08-19 Fable 审查记档，同轮的三条已修见 `docs/CHANGELOG.md`「地图 v1.2」条目）

两条都出在按期结算（`src/sillytavern/map-dynamics.ts` 的 `settleTileFacts` +
`src/sillytavern/state-manager.ts` 的 `syncMapFactsSettlement`），方向一致 —— **丢的一律是玩家该拿的钱，
不会凭空多给**，且都不产生数据损坏：

1. **周期性降档摧毁的玩家产业，摧毁前已到期的收益期数不入账**（`map-dynamics.ts:settleTileFacts`）——
   结算的三步是「周期效果 → 到期移除 → 收益收集」，而收益收集扫的是**活下来的**建筑：一次跨 90 天的
   前进里，若第 30 天那一拍的降档摧毁了最高号槽里的玩家酒馆，它在第 0–30 天本该入的那一期就随它一起
   消失了。**状态到期有中窗折算**（`windowEnd` 压到到期日，那条注释专门讲了这件事），收益没有对应的处理。
   修法要把收益收集与逐期结算**交织**（每跨一期先记这一期的账，再执行这一期的进度效果），
   那会把 `settleTileFacts` 现在这条平铺的三步流程改成逐期循环 —— 与本轮修的三条不同源，故暂记档。
2. **事实态先落库、收益 patch 由锁外自提交消费；自提交失败则该窗口的期数永久丢失**
   （`state-manager.ts:syncMapFactsSettlement` + `applyTimeAdvance` 末尾的自提交）——
   `gameTime` 与事实态在**锁内**已经落库，收益却只是往 `patches[]` 里塞了一条 delta，
   由锁外那次 `commitChatState` 统一提交（锁内嵌套提交 = 同 saveId 自等死锁，这条约束本身是对的）。
   而时间账本是**零簿记推导**的：入账点全靠 `anchorDay` 推，没有「上次结算到哪天」这个游标 ——
   一旦那次自提交失败（抛出，或补丁级失败落进被调用点丢弃的 `errors` 数组），下一次结算的窗口起点
   已经是新的一天，那笔钱再也推不回来，玩家侧只表现为「这个月的产业收益没到」。
   修法方向：给收益补丁一条独立的失败重试/补账通道，或把入账点从「纯推导」改成带一个最小已结算游标
   （后者与 ADR-33「零 lastSettled 簿记」的裁定冲突，需要重新裁定）。

> 📌 **2026-09-05 本条已修**（F08 可恢复收益，落点 `state-manager.ts`「🧾 收益记收账本」节）：
> 收益不再塞 `patches[]` 等锁外自提交 —— 每一笔 `incomeDue` 折叠成**持久借据**
> （`worldFlags.mapIncome`，零新 Dexie 表、随 FullBackup、与事实态同一份快照同袋同滚），
> 与事实态**同一次 profile 落库**（窗口推进与记账同生共死，写失败两者都不落、下次从旧窗口重算）；
> 钱由 `settlePendingMapIncome()` 以「给钱 + 标记 applied 同一 IDB 事务」原子消费 ——
> 崩在任意一点都回到「未给钱 + 未标记」，下次推进自动重放；丢确认后重放读到 applied 即跳过，
> 无重复支付。验证：`src/sillytavern/state-manager.map-income.test.ts` 10 条（写失败重放恰好一次
> / 丢确认重放不重付 / 重复 id / 畸形金额 / 快照回退两刻 / 主建筑 / 零收益 / 分段推进一致性）。
> 🔴 **历史已丢的收益不回溯补偿**（无可靠证据可核算），修复只保未来结算。
> 遗留（如实记录）：`gameTime` 先于结算落库的既有跨度依旧 —— 结算**落库本身**失败时，
> 该窗口的到期/周期/收益一律跳过（那是「时间 vs 事实」的先落跨度，与本条要修的自提交缝隙正交）；
> 第 1 条（降档摧毁前已到期期数不入账）不属 F08 范围，按原裁定保留。

---

# Known issue: 远程素材 v1 审查残留（三条低危，有界不腐蚀数据）

状态：已定位根因、暂不修（2026-08-18，PR #114 合并时 Fable 审查裁定 MERGE 后记档）

三条均出自审查复核，窄边界、计数守恒、不产生数据损坏，修复优先级低：

1. **孤儿队列 FIFO 还账错位**（`src/ui/lib/asset-url.ts:397`）——`evict(id)` 后，同 id **新 URL**
   持有者的 `release` 会先偿还**旧 URL** 的孤儿账。交错序列〔A 持旧 → evict → B 取新 → B 先于 A
   释放〕会提前吊销 A 正在显示的旧 URL（死图），且新 URL 的计数搁浅到 A 释放为止。计数守恒不泄漏；
   代码内注释已声明这是刻意的近似。
2. **evict 微任务窗口**（`asset-url.ts:257/419`）——若 evict 恰落在 `load()` 缓存+持账与发起方
   `liveOnly` 判定之间，发起方拿到 null（不欠账）但孤儿账上多一笔无人偿还的引用，一条 object URL
   滞留到该 id 下一次 release 或 `revokeAll`。有界、自限。
3. **墓碑在声明临时缺席时被误剪**（`src/ui/stores/asset-store.ts:2218`）——墓碑剪枝以**当次**声明集
   为准：世界书被临时停用（或工坊项目关闭）期间跑过一次同步，其槽位的墓碑即被剪掉；重新启用后，
   玩家刻意删除的远程素材会复活。与「作者真的撤回了声明」在当前设计下不可区分。
   后续方向：剪枝改「连续 N 次同步缺席才剪」，或以已安装书集（而非启用条目集）为剪枝键。

背景与修复记录见 `docs/CHANGELOG.md`「远程素材 v1」条目的审查轮小注。

---

# 已关闭记录：UI-01 捏人页禁用装备仍可被选中

状态：已关闭（2026-08-29 产品裁定接受现有行为，无需修复）

来源：`docs/reviews/2026-08-12-ui-review.md` §UI-01（P0，评审原文称「应阻断发布」）。

产品裁定：本项目是单人游戏，创角点数与资格不承担多人公平性、服务器权威或共享经济边界。玩家主动绕过限制只改变自己的开局难度，属于允许的玩家自主选择，不构成缺陷或发布阻断项。

## 技术现象（保留作历史证据）

点数不足 / 种族不符 / 身份不符的物品在装备选择步骤会渲染成灰色禁用卡片，但玩家点卡片**内部**
那颗「选择」按钮仍能把它加进选择集，最终提交出负点数或不满足资格约束的存档。

## 根因

三层校验全部缺席，禁用只是**外观**：

1. `CreateStepSelections.vue:129` 把 `!store.canSelect(item)` 传成卡片的 `disabled` **外观**；
2. `SelectableCard.vue:26` 的卡片级 click 守卫（`!disabled && !selected`）**只守卡片本体**——
   内层「选择」按钮在 `SelectableCard.vue:65` 无条件 `$emit('select', item)`，`@click.stop`
   还顺手挡掉了冒泡，那颗守卫根本轮不到；
3. store 侧不复核：`addEquipment`/`addItem`/`addSkill`（`create-store.ts:703/713/728`）只查
   `isSelected` 去重，**一次都没调 `canSelect`**（`create-store.ts:691`）；
   `CreateStepSelections.vue:43` 的 `handleSelect` 同样是直通。

## 处理结论

不实施代码修复，不再列入发布阻断或缺陷待办。除非未来产品边界发生变化并获得重新授权，不应重新引入强制校验。

---

# Known issue: UI-02 游戏页无窄屏降级，右侧 HUD 被裁掉

状态：未修（2026-08-18 复核）

来源：`docs/reviews/2026-08-12-ui-review.md` §UI-02（P1）。

## 现象

游戏页按固定桌面三栏排版，窄视口下不进任何响应式状态：768px 时右侧 StatusHUD 直接从屏幕右边
消失且无法平移，键盘焦点仍能进到屏幕外的控件；1024px 时中央叙事区已被两侧最小宽度提前挤压。

## 根因

`GamePage.vue:484` 硬编码 `min-width: 900px`，同一条规则里 `width: 100vw` + `overflow: hidden`
（`GamePage.vue:483/487`）——排版按至少 900px 走，却没有任何横向滚动容器，作为末端 flex 子项的
HUD 于是被裁掉。现有 1100px 媒体查询只调内部素材，不改外壳状态。

## 关联

与 `TODO.md`「多分辨率 / 多宽高比适配」是同一条线：那条待办覆盖超宽/竖屏/高 DPI，本缺陷是它在
窄屏一侧最先炸的那个点。修复方向见审查报告 §UI-02（约 1100px 紧凑态 / 约 900px 抽屉化 +
焦点管理）。

---

# Known issue: EJS 世界书与持久角色状态冲突

状态：新存档生成链已修复；旧污染存档仍需重建（2026-08-03）

## 现象

创角时选中含 EJS 的内置命定核心后，Story Agent 可能仍生成与核心世界书不一致的设定。
已观察到的妲丽安案例中，模型把她描述为不知自身来历、近乎无机质的系统核心，而世界书定义的
妲丽安是掌管九十万六百六十六册幻书的「黑之读姬」。

## 当前处理链

世界书进入 Agent 提示词前依次经过：

1. 当前存档的 `metadata.enabledWorldBookEntries` 选择过滤；
2. `AgentConfig.worldBookIds` 的 Agent 可见性过滤；
3. 条目 `enabled` 激活过滤；
4. 静态条目与动态条目分区；
5. 含 `<%` 的动态条目由 QuickJS 预渲染；
6. 渲染结果通过 `{{LORE_BOOK_STATIC}}` / `{{LORE_BOOK_DYNAMIC}}` 注入提示词。

EJS 执行失败时，条目会按原文注入并记录到 Debug 导出的 `ejs.fallbacks`。EJS 世界书的渲染
结果本质上仍是 Agent 提示上下文；除获授权的 `vars` 差量外，它不会自动创建、覆盖或校正
`CharacterState`。

## 已修复部分

内置 `system_core` 和 `character` 条目出厂使用 `enabled: false`，表示默认不全局注入。此前即使
存档明确选择了 `system_core:413`，条目仍会在通用 `enabled` 过滤阶段被删除，根本到不了
QuickJS。

现在，存档明确选中的内置 `system_core` / `character` 条目会在存档过滤边界克隆为启用态。
该覆盖不作用于工坊或用户书；它们仍遵守条目自身的 `enabled` 设置。

后续调试又确认并修复了两处独立问题：

- 旧设置可能保存了一份与引擎内置 Story 默认模板相同的模板。规范预设已经自行注入世界书、历史和
  本轮输入时，这份默认模板会把三者再次追加，导致提示词成倍膨胀。现在保存值与内置默认模板等价
  时按默认模板处理，不再重复装配；真正不同的自定义模板仍保留原行为。
- 当存档明确选择 `system_core:<uid>` 且相应 Agent 的世界书开关启用时，运行时会把
  `system_core` 授予 Story 与 `char_gen`。新配置的 `char_gen` 出厂白名单也已包含
  `system_core`，避免角色生成链只能根据 Story 的二手描述猜测命定核心人格。
- 工坊项目可用保留标签 `system/core` 声明命定核心语义。该项目被当前存档启用的条目会把对应
  `workshop:<projectId>` 授予 Story 与 `char_gen`；未启用条目、普通工坊项目及其他 Agent
  不会因此扩大可见性。标签匹配忽略大小写及首尾空格。

另已修复开场请求重复生成：旧逻辑在整条长管线结束后才写入 `openingPromptConsumed`，页面在管线
期间重新挂载会再次提交同一开场。现在生成前先原子认领；第二个管线实例无法重复启动。

## 尚未解决部分

最新妲丽安调试记录已经满足以下条件：

- EJS 后端为 `quickjs(wasm)`；
- `ejs.fallbacks` 为空；
- Story 提示词包含妲丽安世界书的「九十万六百六十六册幻书」「全知的黑之读姬」等正文。

但该存档在修复前已经由 AI 生成并保存了一个冲突的「妲丽安核心」角色状态。这个状态把她定义为
不知道自身起源的无机质系统，并在 Story 提示词中作为当前角色状态再次注入。模型最终采用了这份
更靠近当前回合的持久状态，而不是世界书设定。

`request_dispatcher` 仍只负责从 Story 正文识别请求，不直接读取 `system_core`；命定核心的权威
原文由 Story 与实际创建角色档案的 `char_gen` 读取。

> 🔴 本节结论**自 2026-08-16「管线并行化」（写队列地基 + 4 层管线 + 侧链旁路化）落地后未重新复核**——
> 那轮改动动了状态落库的时序与顺序，本节的观察是在它之前取的。真机迭代时需要重跑一遍上面三条判据。

## 影响与临时处理

- 修复会让现有存档从下一次提示装配开始收到正确的 EJS 世界书正文，但不会自动修复已经落库的
  冲突角色、任务、记忆或剧情事实。
- 已污染存档需要重建相关命定核心状态，或新建存档后重新验证；仅刷新页面不能清除旧状态。修复
  不会静默覆盖已经发生的剧情和玩家数据。
- 不应通过把内置核心条目的全局 `enabled` 改成 `true` 来规避问题，否则多枚命定核心可能同时注入。

## 后续修复方向

1. 为已有冲突状态提供可控的重建或校正路径，不静默覆盖玩家产生的正常剧情状态；
2. 增加从存档选择、EJS 渲染、角色生成到 Story 回复的端到端真机回归测试；
3. 评估超大 Story 预设的上下文预算，避免非重复内容本身造成注意力稀释。

---

# 2026-08-29 综合审查 P0/P1 登记

状态：审查基线为 `master` @ `7e2cf93`；本次关闭 3 项修复并按产品裁定撤销 2 项，
其余 P1 继续跟踪。

来源：[`docs/reviews/2026-08-29-comprehensive-app-review.md`](reviews/2026-08-29-comprehensive-app-review.md)。

本节只承担长期缺陷登记与状态索引，完整触发条件、可达路径、影响、证据和修复要求保留在审查报告中。
既有 UI-01 与 UI-02 已在本文前文独立登记，不在下表重复分析；正式发布形态继续由 `TODO.md`
“正式发布打包”跟踪，不作为代码缺陷重复登记。

| ID        | 优先级 | 状态               | 缺陷                                                                                                           | 报告位置 |
| --------- | ------ | ------------------ | -------------------------------------------------------------------------------------------------------------- | -------- |
| CRIT-01   | P0     | 已修               | 已删除剧情自然语言触发条件进入宿主域 `new Function` 的执行路径                                                 | §7.1     |
| SEC-01    | P1     | 已修               | FullBackup 已排除本机 API 端点、Key 与可能残留凭据的旧设置行；导入不会覆盖本机凭据                             | §8.1     |
| SEC-02    | P1     | 未修               | 本地 BFF 对普通 Web Origin 开放调用方提供的凭据头转发、私网固定路径代理与内容写回；浏览器跨站利用受 PNA 等限制 | §8.2     |
| ONB-01    | P1     | 已修（2026-09-05） | 首次运行无内容包/API/模型/Agent 就绪门，用户完成创角后才在首回合失败                                           | §8.3     |
| DATA-01   | P1     | 已修（2026-09-05） | 新旅程创建非 single-flight、非事务，失败只写 console，可留下孤儿角色或半份存档                                 | §8.4     |
| LIFE-01   | P1     | 已修（2026-09-05） | 活跃存档存在两个所有者，异步装载无 generation/disposed 栅栏，可跨档提交旧结果                                  | §8.5     |
| LIFE-02   | P1     | 已修（2026-09-05） | Stop 后旧回合尚未完成清理即允许新回合覆盖共享 `GamePipeline` 状态                                              | §8.6     |
| EFFECT-01 | P1     | 已修（2026-09-05） | 效果接线只增不减，已删除或更新的装备、技能和脚本可与权威投影长期漂移                                           | §8.7     |
| STATE-01  | P1     | 已修（2026-09-05） | StateManager 的“全部成功或回滚”契约与部分成功实现冲突，领域调用方仍按原子命令使用                              | §8.8     |
| A11Y-01   | P1     | 已修（2026-09-05） | 共享弹窗、Toast、存档行及创建必选卡片不满足项目声明的键盘与 WCAG 契约                                          | §8.10    |
| BUILD-01  | —      | 已撤销（产品裁定） | 主机上 `public/` 下被 Git 忽略的本地音频可静默进入构建产物并携带未获分发授权内容；干净克隆与 CI 不含该集合     | §8.12    |
| DEV-01    | P1     | 已修（2026-09-05） | 开发启动器会强制终止 5173–5179 上未证明属于本项目的监听进程                                                    | §8.13    |
| TOOL-01   | P1     | 已修               | Node 与 npm 版本已由 package 元数据和 `.nvmrc` 约束，README 改用 `npm ci`                                      | §8.14    |
| GOV-01    | —      | 已撤销（产品裁定） | 远端 `master` 未对代码与配置合并启用分支保护和必需 CI 检查；仓库另有明确的纯 Markdown 直推流程                 | §8.15    |

既有条目映射：

- CRIT-02 → 本文“UI-01 捏人页禁用装备仍可被选中”；2026-08-29 已按产品裁定关闭，不再属于缺陷；
- RESP-01 → 本文“UI-02 游戏页无窄屏降级，右侧 HUD 被裁掉”及 `TODO.md` 的多分辨率/移动端条目；
- REL-01 → `TODO.md`“正式发布打包”。

后续修复必须在同一提交中回写本表状态、修复提交与验证结果；不得删除原始审查分析。

> 2026-09-05：ONB-01 / DATA-01 / LIFE-01 / LIFE-02 已实施，验收与范围见
> `docs/reviews/2026-09-05-first-session-reliability.md`。ONB-01 本轮交付本机就绪检查与设置入口，
> 不自动选模型、不发送连接探测、不估算价格；连接测试复用 API 设置中的现有功能。

> 2026-09-05：上述四项修复的范围与验收见 [剩余四项可靠性修复](reviews/2026-09-05-remaining-reliability.md)。
