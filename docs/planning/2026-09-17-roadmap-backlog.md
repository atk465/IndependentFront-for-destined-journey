# 长期待办：去关联工程与自创前端（2026-09-17 立项）

> 主人裁决：项目改编自《命定之诗》，公开页面标明来源、遵循其内容二创授权协议；
> 仅用于本人与私下朋友非商用游玩。长期方向：逐步剥离与原作的关联，最终以
> 原创世界观 + 自创前端完全独立。本文是两大长期工程的立项清单。

## 工程一：世界观去关联

**目标**：剥离协议 §3「禁止套用命定世界观」涉及的全部核心设定元素，
使《铭刻录》世界观完全自洽独立。

**待替换核心名词清单**（当前全部硬编码/正典化）：

| 命定元素                                                                 | 出现面                                         | 替代方向（待主人命名） |
| ------------------------------------------------------------------------ | ---------------------------------------------- | ---------------------- |
| 奥古斯提姆帝国                                                           | 世界书/canon/事件/委托/agent 提示词/生成器清单 | 待定                   |
| 艾瑟嘉德（默认起始地，**引擎 resetAll 硬编码**）                         | 世界书/catalog 起始地树/地景卡/采集事件        | 待定                   |
| 帝冕币（**开场白硬编码**"帝冕币"；variable 书口径；帝冕雉/铭冕纹派生词） | buildOpeningPrompt/世界书/委托赏金             | 待定                   |
| 瓦伦蒂亚公国 / 诺瓦·瓦伦蒂亚城                                           | 世界书/catalog 起始地树/背景/地景卡            | 待定                   |
| 女帝/铭冕纹/帝都·冕京                                                    | 世界书正典                                     | 待定                   |
| 血脉集 28 族描述中的命定痕迹排查                                         | bloodlines.json                                | 待排查                 |

**影响面**：worldbooks 14 本 / catalog（起始地树·背景·委托·事件文案）/ canon /
buildOpeningPrompt（货币名硬编码）/ resetAll（默认地点硬编码）/ 名所地景卡命名。
**前置**：主人提供替代命名体系；**执行**：批量替换 + 世界书条目重写 + 生成器重跑。

## 工程二：自创前端（脱离命定底包）

**目标**：引擎骨架逐步重写为原创实现，彻底解协议 §3「框架条款」与 §5「预设条款」。

**现状盘点**（继承自命定前端的框架面）：
装备品质七级/技能体系/层级 tier/五维/HP·MP·SP 公式/世界书架构/状态栏正则/vars 系统/story-preset。

**路径建议**：模块级渐进重写（每步有测试垫底）——
状态栏正则 → 世界书架构 → 数值框架 → 战斗规则 → 预设体系。
配套自研预设替代占位 story-preset（当前占位预设改自命定专用预设，§5 标明来源使用中）。

## 工程三：剩余未实装天赋（天赋系统收尾）

**现状**（2026-09-17 收尾时点）：SSS 47/49、SS 35/46、S 59/99、全目录 386/698
有至少一条已实装机制。SSS 剩两条是**纯名字钩子**（效果生效但判定走天赋名，
故不计入条目统计），不欠工。

### 已确认需要「跨战斗账本」的三条 SS（建议一次做）

一拳超人系统 / 败犬烙印 / 好运之骰 都需要一个**跨战斗的持久账本**，
建在 `SaveProfile` 上（先例：`narrativeIntents`、情绪素材的 `lastExtractDay`）。

| 天赋 | 需要账本记什么 | 还需要什么 |
| ---- | -------------- | ---------- |
| 一拳超人系统 | 今日是否已出拳（gameDay） | 出拳后 24h 虚弱——状态效果落地 |
| 败犬烙印 | 烙印累计数（战败 +1） | 制卡时消耗一枚扭转一次词条冲突（接 craft 流程） |
| 好运之骰 | 今日是否已投骰（gameDay） | 十面骰十个结果各自的兑现方式（**待主人定**） |

**设计决策待定**：好运之骰的十个结果里，「提升一级」「刀刀暴击」等各自要接
不同的系统（升级/战斗暴击/素材掉落），需要先定哪些走 Code、哪些走叙事。

### 已裁定：好运之骰的十面兑现口径（2026-09-17）

**裁定**：十面 = 十条数据，每面自带兑现口径，**全部 Code 兑现**，AI 只负责把它
写成一段像命运的话。理由：抽奖是本作唯一的纯随机入口，玩家会反复投，结果必须
可复算、可审计，不能靠叙事手感。

| 点 | 面 | 吉凶 | 兑现 |
| -- | -- | ---- | ---- |
| 1 | 杂鱼杂鱼 | 凶 | 金钱 −50（clamp ≥0） |
| 2 | 谢谢惠顾 | 平 | 无 |
| 3 | 再来一次 | 吉 | **退还今日次数**（不写账本） |
| 4 | 福缘天降 | 吉 | 金钱 +150 |
| 5 | 制卡顺利 | 吉 | 当日制卡评级上浮一档（大失败→失败→成功→精益求精） |
| 6 | 红鸾天喜 | 吉 | 好感最高的那位 +5 |
| 7 | 材料秘境 | 吉 | 稀有素材 ×3（与拆解同一命名口径） |
| 8 | 刀刀暴击 | 吉 | 当日战斗行动值 ×1.25 |
| 9 | 提升一级 | 大吉 | 等级 +1 |
| 10 | 屠龙宝刀 | 大吉 | 星辉档卡牌一张（复用祭坛抽卡内核） |

点数越大越好（玩家一眼能读）。5/8 两面是**当日增益**，走
`worldFlags.dailyBuffs.<key> = gameDay`，跨天自动失效。
本条落地后，`日掷` 这个条目种类可被 S「命运之骰」（六面）直接复用——
只需补一张六面表并给该种类加 `faces` 档位。

### 已落地：命运之骰 / 败犬烙印 / S 近似复用三条（2026-09-17）

- **命运之骰（S，六面）**：与 SS「好运之骰」共用 `日掷` 条目种类，靠 `faces` 档位
  分派到不同面表（6 = 命运之骰纯吉凶梯度 / 10 = 好运之骰）。两张骰子可同时持有，
  面板一次列全，各记各的今日次数。
- **败犬烙印（SS）**：战败 +1 走**累计计数**（`worldFlags.counters`，不随天失效，
  上限走 `烙印{maxHold}`）；制卡时消耗一枚把评级上浮**两档**（比「制卡顺利」的一档
  更狠，对应「化腐朽为神奇」）。玩家侧用**预付开关**（`worldFlags.pendingScar`）——
  制卡由叙事驱动，玩家无法在那一刻点按钮，所以先勾上、下一次制卡自动扣。
- **S 近似复用三条**：无垢仙体（东方）→ `剥离`；痛苦阶梯 → `自我进化`；
  不等价交换 → `置换`（新通道，`unequal-exchange.ts`：放弃一件素材/卡牌换回
  1~2 个同类同品质回报，**换亏是设计的一部分**）。

**未做：雌雄双煞**——它的触发条件是「队伍中恰好一名异性伙伴卡 **且** 双方都在被通缉」。
前半句需要队伍构成判定，后半句「被通缉」**引擎里没有任何表示**（只在禁忌仿卡惩罚里
作为文案出现过）。硬做只能做成无条件 +50% 全属性，那是错的强度。等通缉状态有落点再说。

### 已落地：新管道首批三条（2026-09-17）

- **抽奖内核（`material-gacha.ts`）**：素材十连——`times` 发按权重滚稀有度，
  **保底不靠「最后一发必出」而是抽完补齐**（把最差的一发抬到保底档，前面抽得
  好也不吃亏），突变面 3% 出带高阶突变词条的素材。保底档由等级经
  `tierForLevel` → `TIER_TO_RARITY` 换算（与拆解/转化同源，不另起等级口径）。
  顺带把 `TIER_TO_RARITY` 的两份副本合成一份（原在 card-dismantle 与
  companion-capture 各有一份）。
- **免死 / 复活（两条通道，分工不同）**：
  - `免死`（绞刑架幸存者）= **战中**：HP 会被打到 0 时锁血到 `hpFloor` 续战，
    每场一次（会话级守卫，与 `nukeUsed` 同款）；`mpRefill` 决定是否补满 MP。
  - `复生`（再生）= **战后**：败北结算时把 HP 抬到 `hpFloor`——不死之身，只是输了。
- **自身战斗状态（`self-status.ts`）**：玩家侧被动，开战即生效整场。
  状态名 → 机械形态走表：`隐身` → `threatDown`（并进威压同一个百分比一起缩放敌方
  意图）；`吸魔` → `regen`（新增拍内效果类型，每拍末回 HP、封顶上限）。
  认不出的状态名不进列表——不给引擎看不懂的状态占位。
  `startSkirmish` 新增 `initialEffects`，让开战就带在场效果成为可能。

**未做：亲吻女神系统**——奖励品质要按「对方实力 × 颜值 × 好感」算，其中**颜值
引擎里没有数值**（`appearance` 只是一段文本描述）。硬做只能退化成「实力 × 好感」，
那就不是描述里那个天赋。等颜值有落点（例如角色卡加一个数值字段）再说。

### 需要独立管道的八条 SS

| 天赋 | 缺什么 | 量级 |
| ---- | ------ | ---- |
| 天生剑骨（东方）/ 战意破苍穹 | 本名卡（随角色成长）+ 装备槽限制 + 因果绑定 | **大**（一次解锁两条） |
| 卡组生态 | 卡组自繁衍/捕食/进化的整套子系统 | 大 |
| 鬼话连篇 | 叙事产物 → 伙伴卡的入口（现 capture 被「战胜」门槛挡着） | 中 |
| 爱 | 免材料首召 + 性转同体 + 双向同步强化 | 中 |
| 绝对母权 | 伙伴战斗行为域（拉仇恨/保护/惩戒）——现伙伴卡只有 buff 数值 | 中 |
| 千秋证果 | 升级阈值/经验储存/手动升级/经验分享；**引擎无「升级阈值」这个量**，只给属性加成会破坏其取舍设计 | 中 |
| 永恒航路 | 海图 + 航速加成，属地图/旅行域，与战斗天赋不同体系 | 中 |

### 分类修正记录（避免重复评估）

- **画师** 原列「需新管道」，实为复用 `改造` 通道（`planReshape`）——已落地。
- **词条窃贼** 原列「需新管道」，实为复用 `吞噬` 语义——已落地。
- **退化射线** 原列「需新管道」，实为 `战技附加` 未实装所致——已随该条目落地。
- **卡牌宗师** 曾被误判为「零条目」（提取脚本漏了 `entry(...)` 辅助写法），实为早已实装。

## 其他路线图遗留

- 1.1 L2：AI 意图解析（自由文本含糊表达出卡，需新代理契约设计）
- 5 数值平衡：DC 表/槽位成本/反噬伤害/战力公式/开局卡价/天赋品级定价全部为初稿口径
- 6 上游 PR：已按主人裁决永久关闭；origin = atk465 仓库为唯一真源
